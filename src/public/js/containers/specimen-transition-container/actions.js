export const initTransition = (data) => ({
  type: 'INIT_TRANSITION',
  data
})

export const startTransition = (data) => ({
  type: 'START_TRANSITION',
  data
})

export const endTransition = (data) => ({
  type: 'END_TRANSITION',
  data
})
